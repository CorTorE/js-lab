import RenderCounter from "./RenderCounter";

const Message: React.FC = () => {
  return (
    <RenderCounter name="Message 컴포넌트">
      <p>안녕하세요! 저는 Message 컴포넌트입니다.</p>
    </RenderCounter>
  );
};

export default Message;
